name: Build & Deploy Unity WebGL

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true

      # Cache Unity Library (speeds up future builds)
      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: "Game Studio II/Game Studio II/Library"
          key: Library-${{ runner.os }}-${{ hashFiles('Game Studio II/Game Studio II/Packages/manifest.json', 'Game Studio II/Game Studio II/ProjectSettings/ProjectVersion.txt') }}
          restore-keys: |
            Library-${{ runner.os }}-

      - name: Build Unity WebGL
        uses: game-ci/unity-builder@v4
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        with:
          projectPath: "Game Studio II/Game Studio II"
          targetPlatform: WebGL

      - name: Upload WebGL build to Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: "Game Studio II/Game Studio II/build/WebGL/WebGL"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
